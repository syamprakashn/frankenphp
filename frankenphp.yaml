frankenphp:
  version: "1.0"
  config:
    server:
      listen:
        - ":443"
      tls:
        certificates:
          - domain: localhost
            self_signed: true
    app:
      document_root: /app/public
